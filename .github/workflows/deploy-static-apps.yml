name: Deploy Static Apps

on:
  push:
    branches:
      - main
      - testing
  workflow_dispatch:

concurrency:
  group: deploy-static-apps-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy_public:
    name: Deploy Public App
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    env:
      VITE_CONTENT_PACK: public
      VITE_SITE_ORIGIN: https://thankful-bush-0d8565003.1.azurestaticapps.net
      VITE_ENABLE_AI_OBSERVABILITY: "false"
      GITHUB_SHA: ${{ github.sha }}
      GITHUB_RUN_NUMBER: ${{ github.run_number }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false

      - name: Build and deploy public app
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_THANKFUL_BUSH_0D8565003_1 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: fredagskoll-frontend
          api_location: api
          output_location: dist

  deploy_team:
    name: Deploy Team App
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    env:
      VITE_CONTENT_PACK: team
      VITE_SITE_ORIGIN: https://delightful-ground-0b3aa2b03.4.azurestaticapps.net
      VITE_ENABLE_AI_OBSERVABILITY: "false"
      GITHUB_SHA: ${{ github.sha }}
      GITHUB_RUN_NUMBER: ${{ github.run_number }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false

      - name: Build and deploy team app
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_DELIGHTFUL_GROUND_0B3AA2B03 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: fredagskoll-frontend
          api_location: api
          output_location: dist

  deploy_testing:
    name: Deploy Testing App
    if: github.ref == 'refs/heads/testing'
    runs-on: ubuntu-latest
    env:
      VITE_CONTENT_PACK: public
      VITE_SITE_ORIGIN: ${{ vars.VITE_SITE_ORIGIN_TESTING }}
      VITE_ENABLE_AI_OBSERVABILITY: "true"
      GITHUB_SHA: ${{ github.sha }}
      GITHUB_RUN_NUMBER: ${{ github.run_number }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false

      - name: Build and deploy testing app
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_TESTING }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          app_location: fredagskoll-frontend
          api_location: api
          output_location: dist
